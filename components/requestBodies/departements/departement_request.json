{
    "DepartementRequest": {
        "required": [
            "name"
        ],
        "type": "object",
        "properties": {
            "name": {
                "$ref": "../../schemas/string.json#/String",
                "example": "{{$randomDepartment}}",
                "default": "Production",
                "description": "Name of the departement"
            }
        }
    }
}